#include <stdio.h>
int main()
{
    int i,n,max;
    printf ("Enter the n limit: ");
    scanf ("%d",&n);
    int arr[n];
    printf ("Enter %d different numbers: ",n);
    for (i=0; i<n; i++)
    {
        scanf ("%d",&arr[i]);
    }
    max=arr[0];
    for (i=0; i<n; i++)
    {
        if (arr[i]>max)
        {
            max=arr[i];
        }
    }
    printf ("The Greatest number among %d number: %d\n",n,max);
    return 0;
}
