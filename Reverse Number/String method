#include <stdio.h>
#include <string.h>
int main()
{

    char num[10];
    printf ("Enter the number: ");
    gets (num);
    strrev (num);
    printf ("The reverse number is: %s",num);
    return 0;
}
