#include <stdio.h>
#include <stdbool.h>
int main()
{
    int n;
    printf("Enter n terms: ");
    scanf("%d",&n);
    printf("Self-divisible numbers up to %d is:\n",n);
    for (int i=1; i<=n; i++)
    {
        int m=i;
        bool ans=true;
        while (m>0)
        {
            int d=m%10;
            if (d==0 || i%d!=0)
            {
                ans=false;
                break;
            }
            m/=10;
        }
        if (ans)
            printf("%d ",i);
    }
    return 0;
}
