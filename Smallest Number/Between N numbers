#include <stdio.h>
int main()
{
    int i,n,min;
    printf ("Enter the n limit: ");
    scanf ("%d",&n);
    int arr[n];
    printf ("Enter %d different numbers: ",n);
    for (i=0; i<n; i++)
    {
        scanf ("%d",&arr[i]);
    }
    min=arr[0];
    for (i=0; i<n; i++)
    {
        if (arr[i]<min)
        {
            min=arr[i];
        }
    }
    printf ("The Smallest number among %d number: %d\n",n,min);
    return 0;
}
