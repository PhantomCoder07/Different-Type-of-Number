#include <stdio.h>
int main()
{
    int a,b,c;
    printf ("Enter three integers: ");
    scanf ("%d %d %d",&a,&b,&c);
    if (a<=b)
    {
        if (a<=c)
            printf ("%d is Smallest.",a);
        else
            printf ("%d is Smallest.",c);
    }
    else
    {
        if (b<=c)
            printf ("%d is Smallest.",b);
        else
            printf ("%d is Smallest.",c);
    }
    return 0;
}
