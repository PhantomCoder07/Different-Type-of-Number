#include <stdio.h>
int small (int a, int b)
{
    if (a<b)
        return a;
    else
        return b;
}
int main()
{
    int a,b;
    printf ("Enter two different numbers: ");
    scanf ("%d %d",&a,&b);
    printf("%d is Smallest\n",small(a,b));
    return 0;
}
