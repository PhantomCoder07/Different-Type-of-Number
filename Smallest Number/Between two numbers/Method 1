#include <stdio.h>
int main()
{
    int a,b;
    printf ("Enter two numbers: ");
    scanf ("%d %d",&a,&b);
    if (a<b)
        printf("%d is Smallest.\n",a);
    else if (b<a)
        printf("%d is Smallest.\n",b);
    else
        printf ("Both are Equal.\n");
    return 0;
}
